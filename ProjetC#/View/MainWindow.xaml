<Window x:Class="Game.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Height="750" Width="1250"
        ResizeMode="NoResize">

    <Window.Resources>
        <Storyboard x:Key="monsterAnimation">
            <DoubleAnimation Storyboard.TargetName="monsterImage"
                     Storyboard.TargetProperty="(Image.RenderTransform).(TranslateTransform.Y)"
                     From="0" To="10" Duration="0:0:0.6"
                     AutoReverse="True" RepeatBehavior="Forever"/>
        </Storyboard>

        <Storyboard x:Key="monsterAnimationn">
            <DoubleAnimation Storyboard.TargetName="monsterImage1"
             Storyboard.TargetProperty="(Image.RenderTransform).(TranslateTransform.Y)"
             From="0" To="10" Duration="0:0:0.6"
             AutoReverse="True" RepeatBehavior="Forever"/>
        </Storyboard>





    </Window.Resources>



    <Grid Height="1230">
        <!--Set background image with animation support 
        <Image x:Name="animatedImage" Margin="75,9,453,151">
            <Image.Source>
                <BitmapImage UriSource="/photos/gggg.gif" CacheOption="OnLoad" />
            </Image.Source>
        </Image>-->

        <Image Source="/photos/menu.png" Stretch="UniformToFill" Margin="0,-56,0,0" />

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

            <Button Content="Jouer" Width="76" Click="JouerButton_Click" Height="25" RenderTransformOrigin="0.572,-6.68" />

            <Button Content="Quitter" Width="76" Click="QuitterButton_Click" Height="25" Margin="0,10,0,0" />
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
        </Grid>

        <Image Margin="658,250,658,744" Source="/photos/Chainsaw-Man-Logo.png" Stretch="Fill"/>

        <Image x:Name="monsterImage" Margin="119,0,0,548" Source="/photos/2.png" Stretch="Fill" Height="297" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="266">
            <Image.RenderTransform>
                <TranslateTransform/>
            </Image.RenderTransform>
        </Image>

        <Image x:Name="monsterImage1" Margin="906,0,0,587" Source="/photos/pochita.png" Stretch="Fill" Height="148" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="233">
            <Image.RenderTransform>
                <TranslateTransform/>
            </Image.RenderTransform>
        </Image>

    </Grid>
</Window>